University Sentiment Analysis project - 
Project which takes in a dataset of text coupled with the sentiment (positive, negative, neutral) associated with it, and uses that to train a model which then predicts the sentiment of a dataset of text without sentiment associated with it.
